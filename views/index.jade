extends layout

block page
  - var menu = 'index'
block content
  if (items.length === 0)
    | There's no published content yet. 
    a(href="/login") Log in
    |  to post and publish.
  else 
    each item, index in items
      div
        h2
          a(href="/items/#{item.Bmodel}")= item.Bmodel